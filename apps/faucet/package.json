{
  "name": "faucet",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bunx wrangler@latest --config='wrangler.json' dev",
    "deploy": "bunx wrangler@latest --config='wrangler.json' deploy --keep-vars"
  },
  "dependencies": {
    "@porto/apps": "workspace:*",
    "porto": "workspace:*",
    "viem": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250412.0",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "wrangler": "^4.10.0"
  }
}
